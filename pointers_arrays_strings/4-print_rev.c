#include "main.h"
/**
 * print_rev - lelele
 *
 * @s: ueueue
 *
 */
void print_rev(char *s)
{
	int nya;

		for(nya = 0; s[nya]; nya++)
		{
			_putchar(s[nya]);
		}
		for(ayn = 0; s[ayn]; ayn++)
		{
			_putchar(s[ayn]);
		}
			_putchar('\n');
}
