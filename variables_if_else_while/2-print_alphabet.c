#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char hola;

	for (hola = 'a'; hola <= 'z'; hola++)
{
		putchar(hola);
		putchar('\n');
		return (0);
}
